# Function to find the minimum of three numbers
min <- function(a, b, c) {
  return(min(a, b, c))
}

# Clear workspace 
rm(list=ls())

# Enter values
a <- 5
b <- 10
c <- 4

  min_value <- min(a, b, c)
  print(paste("The minimum value is", min_value))
